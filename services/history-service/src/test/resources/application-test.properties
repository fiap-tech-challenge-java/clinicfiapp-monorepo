server.port=8083
spring.application.name=history-service

# H2 Database para testes - Modo PostgreSQL
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.flyway.enabled=false

# Kafka configuração para testes
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=history-consumers-test
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=true

# GraphQL
spring.graphql.graphiql.enabled=false
spring.graphql.path=/graphql

# Management
management.endpoints.web.exposure.include=health

# Security - Mock JWT key for tests
api.security.token.public-key=-----BEGIN PUBLIC KEY-----\nMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA0Z7F3VvGm8WT4yXKEqQF\n-----END PUBLIC KEY-----

